# A3S Box Queue - Justfile

# Default recipe
default:
    @just --list

# Build
build:
    cargo build -p a3s-box-queue

# Build in release mode
build-release:
    cargo build -p a3s-box-queue --release

# Run tests
test:
    cargo test -p a3s-box-queue --lib

# Run tests with output
test-verbose:
    cargo test -p a3s-box-queue --lib -- --nocapture

# Run a specific test
test-one TEST:
    cargo test -p a3s-box-queue --lib -- {{TEST}}

# Format code
fmt:
    cargo fmt -p a3s-box-queue

# Check formatting
fmt-check:
    cargo fmt -p a3s-box-queue -- --check

# Run clippy
lint:
    cargo clippy -p a3s-box-queue --all-targets -- -D warnings

# Run clippy with fixes
lint-fix:
    cargo clippy -p a3s-box-queue --all-targets --fix --allow-dirty

# Check compilation
check:
    cargo check -p a3s-box-queue

# Generate documentation
doc:
    cargo doc -p a3s-box-queue --no-deps --document-private-items

# Open documentation
doc-open:
    cargo doc -p a3s-box-queue --no-deps --open
