# A3S Box Python SDK - Justfile
# Most commands available via root: just test, just fmt, just lint

default:
    @just --list

# Build with maturin (dev)
build:
    maturin develop

# Build release wheel
build-release:
    maturin build --release

# Run tests (this crate only)
test:
    cargo test -p a3s-box-sdk-python --lib
